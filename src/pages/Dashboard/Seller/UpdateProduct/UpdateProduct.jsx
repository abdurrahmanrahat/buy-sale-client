import { useLoaderData } from "react-router-dom";

const UpdateProduct = () => {
  const product = useLoaderData();
  // console.log(product);
  return <div className="text-3xl font-semibold">UpdateProduct function will be coming soon!!</div>;
};

export default UpdateProduct;
